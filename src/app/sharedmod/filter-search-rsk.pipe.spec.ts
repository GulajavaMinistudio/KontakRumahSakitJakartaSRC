import { FilterSearchRskPipe } from './filter-search-rsk.pipe';

describe('FilterSearchRskPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterSearchRskPipe();
    expect(pipe).toBeTruthy();
  });
});
