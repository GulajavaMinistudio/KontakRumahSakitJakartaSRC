import { FilterSearchRsuPipe } from './filter-search-rsu.pipe';

describe('FilterSearchRsuPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterSearchRsuPipe();
    expect(pipe).toBeTruthy();
  });
});
