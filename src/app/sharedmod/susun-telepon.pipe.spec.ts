import {SusunTeleponPipe} from './susun-telepon.pipe';

describe('SusunTeleponPipe', () => {
  it('create an instance', () => {
    const pipe = new SusunTeleponPipe();
    expect(pipe).toBeTruthy();
  });
});
